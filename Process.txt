需要实现的交互步骤。

var array = document.querySelectorAll('.card');
console.log (array);

创建出代表卡片的 HTML。

  -用object元素来代表每张卡片。用id来表示每个元素
    *卡片状态：card／card.show／card.match/card.open（保持正面朝上，不因点击而翻面）用不同的class 分开
  -作用于整体
    flip function(每个卡片元素)

添加处理点击操作的功能。
这样应该会显示每张卡片的底面。
  -点击翻转底面
  -每次点击记录点击次数
    -star颜色相应改变（随步数变多而减少）
    -1 Move -Multiple moves
处理匹配逻辑。游戏如何知道玩家猜对了，还是猜错了？
  -对比这一张和上一张
    -相同猜对
      *猜对的动画+底色改变
    -不同猜错
      *猜错的动画+底色改变
      *卡片翻转回到之前的位置
处理获胜条件。游戏如何知道玩家是否获胜了？
  -获胜动画
  -获胜弹出提示窗口（总结游戏结果）
  -再来一局，回到初始界面

刷新游戏
  -按刷新按钮后重新洗牌。 *save deck as a const array? and re-arrange?
  -star与move记录reset


我们建议到最后阶段再去调整格式& css style
